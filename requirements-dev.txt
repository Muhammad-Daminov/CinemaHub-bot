# Development and CI dependencies. Runtime deps live in requirements.txt.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
# Drives the ASGI app in endpoint tests (tests/test_watch_endpoint.py).
httpx>=0.27
